using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Zikkurat.Managers
{
    public class SpawningManager : MonoBehaviour
    { /*
        private string terran_Path = "Prefabs/FiremanEmp";
        private string zerg_Path = "Prefabs/ZergEmp";
        private string protoss_Path = "Prefabs/ZealotEmp";
        private List<Building> _buildings = new();
        private void Awake()
        {
            _buildings.AddRange(FindObjectsOfType<Building>());
        }
        // Start is called before the first frame update
        void Start()
        {
            StartCoroutine(UnitsSpawning());
        }

        // Update is called once per frame
        void Update()
        {

        }
        private IEnumerator UnitsSpawning()
        {
            while (true)
            {
                foreach(var building in _buildings)
                {
                    //building._frequencyOfSpawning -= Time.deltaTime;
                        
                }
            }
        } */
    }
}
